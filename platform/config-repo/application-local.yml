app:
  platform:
    control-plane:
      config-server-url: ${APP_CONFIG_SERVER_URI:http://localhost:8888}
      eureka-default-zone: ${APP_EUREKA_DEFAULT_ZONE:http://localhost:8761/eureka}
  secrets:
    local-dir: ${APP_LOCAL_SECRETS_DIR:${user.dir}/../platform/secrets/local}
