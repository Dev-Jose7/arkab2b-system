spring:
  r2dbc:
    url: ${APP_DOCKER_R2DBC_URL:r2dbc:postgresql://postgres:5432/inventory}
    username: ${APP_DOCKER_R2DBC_USERNAME:inventory}
    password: ${APP_DOCKER_R2DBC_PASSWORD:inventory}
  data:
    redis:
      host: ${APP_DOCKER_REDIS_HOST:redis}
      port: ${APP_DOCKER_REDIS_PORT:6379}
      password: ${APP_DOCKER_REDIS_PASSWORD:}
  kafka:
    bootstrap-servers: ${APP_DOCKER_KAFKA_BOOTSTRAP_SERVERS:kafka:9092}
    client-id: ${APP_DOCKER_KAFKA_CLIENT_ID:inventory-service-docker}

app:
  database:
    schema:
      initialize-on-startup: ${APP_DATABASE_SCHEMA_INITIALIZE_ON_STARTUP:true}
